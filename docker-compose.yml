services:
  keyframe-extract:
    build: .
    ports:
      - "8088:8000"
    environment:
      VIDEO_ROOT: /videos
      OUTPUT_DIR: /output
      CACHE_DIR: /cache
    volumes:
      - ./videos:/videos:ro
      - ./output:/output
      - ./.cache:/cache
    restart: unless-stopped
